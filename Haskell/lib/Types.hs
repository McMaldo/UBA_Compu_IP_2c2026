module Types where

import Data.Complex (Complex)

type N = Word

type Z = Integer
type ZxZ = (Z, Z)
type ZxZxZ = (Z, Z, Z)

type Q = Rational

type R = Float
type RxR = (R, R)
type RxRxR = (R, R, R)

type C = Complex R

type B = Bool
